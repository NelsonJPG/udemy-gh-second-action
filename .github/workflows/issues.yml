name: Issues Workflow
on: issues
jobs:
    output-info:
        runs-on: ubuntu-latest
        steps:
            - name: Get the code in repo
              uses: actions/checkout@v3
            - name: Output issue information
              run: |
                echo "${{ toJSON(github.event.issue) }}"